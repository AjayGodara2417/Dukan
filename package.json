{
 "name": "dukan-fullstack",
 "private": true,
 "scripts": {
   "build": "npm install && npm install --prefix client && npm run build --prefix client",
   "start": "node backend/index.js"
 },
 "dependencies": {
   "express": "^4.18.2"
 }
}
